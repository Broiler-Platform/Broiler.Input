<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <RootNamespace>Broiler.Input.Keyboard.Android</RootNamespace>
    <AssemblyName>Broiler.Input.Keyboard.Android</AssemblyName>
    <PackageId>Broiler.Input.Keyboard.Android</PackageId>
    <Description>Android KeyEvent keyboard input provider for Broiler.</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- The test runner reaches the internal translation helpers for focused coverage. -->
    <InternalsVisibleTo Include="Broiler.Input.Android.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Broiler.Input.Keyboard\Broiler.Input.Keyboard.csproj" />
    <ProjectReference Include="..\Broiler.Input.Android\Broiler.Input.Android.csproj" />
  </ItemGroup>

</Project>
