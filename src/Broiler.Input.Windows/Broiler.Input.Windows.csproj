<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>Broiler.Input.Windows</RootNamespace>
    <AssemblyName>Broiler.Input.Windows</AssemblyName>
    <PackageId>Broiler.Input.Windows</PackageId>
    <Description>Shared Windows host, clock, and native lifetime support for Broiler input providers.</Description>
    <Configurations>Debug;Release</Configurations>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Broiler.Native.Windows" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Broiler.Input\Broiler.Input.csproj" />
  </ItemGroup>

</Project>
